# LLM Wiki 极简实操教程

本教程将引导你理解并运作你刚刚初始化的 **LLM Wiki** 知识库。

> **核心哲学**：**“停止检索，开始编译 (Stop retrieving. Start compiling.)”**。
> 传统的 RAG 只是在需要时临时抓取碎片，而 LLM Wiki 则是让 AI 将原始碎纸片（Raw Sources）融汇贯通，编译成一份像软件代码一样精心组织、持续演进的 Markdown 个人百科（The Wiki）。

---

## 1. 知识库架构一览

打开你的 Obsidian 软件，并选择打开当前 `llm-wiki` 文件夹作为一个仓库 (Vault)。你会看到以下结构：

1.  **`raw/` 文件夹（只读源）**：
    *   **职责**：存放你的第一手资料，如论文 PDF、剪藏的网页、播客逐字稿、乱七八糟的随手记。
    *   **原则**：一旦存入，**AI 只能读取，绝不能修改**。它是我们核对事实的最终依据。
2.  **`wiki/` 文件夹（AI 编译的百科）**：
    *   **职责**：存放由 AI 精炼出的网状百科页面。每一页都高度聚焦于一个概念（如 `[[费曼学习法]]`）或实体。
    *   **原则**：**由 AI 写入和维护，你来阅读和启发**。
3.  **`schema.md`（运行规范）**：
    *   **职责**：这是 AI 助手的“说明书”。当你在使用任何 AI 时，直接将本文件提供给它，它就会自动遵循整套构建规范。
4.  **`index.md`（全局索引）**：
    *   **职责**：类似于书籍的目录。AI 每次新增页面都会在这里更新，方便你和 AI 快速纵览全局。
5.  **`log.md`（更新日志）**：
    *   **职责**：按时间顺序记录你对知识库的操作（如摄入了什么，进行了什么分析），方便日后追溯。

---

## 2. 核心操作实操指南

这是你与 AI（如 Antigravity，或本地运行的 Claude Code 等）协同工作的三个黄金指令。

### 🛠️ 操作一：摄入 (Ingest) — 编译新知识
**场景**：你刚刚往 `raw/` 放入了一份新论文、或者一篇文章。你想让 AI 提取新知识并融入你的 Wiki 库。

👉 **对 AI 发送的指令模板**：
> “我已经将 `<新文件名>.md` 放到了 `raw/` 目录中。请阅读 `schema.md` 中的规范，然后帮我摄入（Ingest）这个新资料。
> 1. 阅读 `raw/<新文件名>.md`，提取核心概念和实体。
> 2. 更新或新建 `wiki/` 目录下的相关页面，使用 `[[双向链接]]` 建立与现有概念的关联。
> 3. 更新全局索引 `index.md`。
> 4. 在 `log.md` 底部追加你的更新记录。”

*💡 此时你会看到：AI 开始自动读写多个文件，把零碎的内容融汇到你已有的知识网络中。*

---

### 🔍 操作二：查询 (Query) — 基于 Wiki 深入思考
**场景**：随着你累积了十多篇不同的相关文章，你想让 AI 总结出它们之间的核心逻辑或做个对比。

👉 **对 AI 发送的指令模板**：
> “请阅读 `index.md`，帮我查询与 `<某个主题/概念>` 相关的 Wiki 页面。
> 我想知道这些概念之间有什么关联/差异？请生成一份对比表格。
> （如果回答非常精彩，请在 `wiki/` 下直接新建一个页面如 `wiki/<主题对比>.md` 将其沉淀下来，并更新 `index.md`）”

*💡 此时你会看到：AI 不需要去翻成百上千页的原始粗糙文本，而是直接阅读它已经精心整理好的 Wiki 页面，从而生成极其深刻、结构化的多文档合成分析。*

---

### 🧹 操作三：健康检查 (Lint) — 消除“知识 Rot”
**场景**：知识库变大后，可能会出现信息打架、链接失效、孤立页面的情况。通常每周或每两周让 AI 运行一次健康检查。

👉 **对 AI 发送的指令模板**：
> “请帮我运行一次 Lint（健康检查）操作：
> 1. 扫描整个 `wiki/` 目录，找出没有任何入站链接的‘孤儿页面’，在合适的地方建立连接。
> 2. 检查是否有频繁提到但在 `wiki/` 下还没有专属页面的‘缺失概念’，向我推荐是否创建它们。
> 3. 检查不同 Wiki 页面之间是否存在信息冲突或前后不一致，如果存在，请生成一份报告，并用 `> [!WARNING]` 在对应文件里进行标注。”

---

## 3. Obsidian 中的高级食用技巧 (IDE 玩法)

Karpathy 提出：“Obsidian 是 IDE；LLM 是程序员；wiki 是代码库”。建议在 Obsidian 中开启以下功能：

1.  **关系图谱 (Graph View)**：
    *   在 Obsidian 左侧边栏点击“关系图谱”图标。
    *   随着你摄入的资料变多，你会看到一个个小点通过线条纵横交错。这能极其直观地向你展示你的“知识版图”——哪些是核心枢纽，哪些是分支。
2.  **双向链接快速跳转**：
    *   在 Wiki 页面中，按住 `Cmd` (Mac) 或 `Ctrl` (Windows) 并点击 `[[双向链接]]`，可以直接跳转至对应页面。如果页面不存在，会自动创建。
3.  **大纲视图与反向链接 (Backlinks)**：
    *   在 Obsidian 右侧边栏开启“反向链接”面板。当你在阅读一个概念时，可以瞬间看到“有哪些其他页面引用了当前页面”，极大地帮助你建立知识联想。
4.  **结合 Dataview 插件**（推荐高级用户）：
    *   在 Obsidian 社区插件中搜索安装 `Dataview`。
    *   你可以在页面中写一段简单的查询语句，自动把 `wiki/` 下所有带有 `tags: [思维模型]` 且在最近一周内更新的页面以表格形式列出来，无需手动维护列表。

---

## 4. 常见问题 (FAQ)

*   **Q: 我可以直接修改 `wiki/` 下的页面吗？**
    *   *A: 当然可以！这是你的个人知识库。当你发现 AI 整理得不对，或者你有新的灵感时，随时可以直接动手编辑。在下一轮 AI Ingest 时，AI 会基于你修改后的页面进行增量更新。*
*   **Q: 为什么不直接用向量检索（RAG）？**
    *   *A: RAG 适合从上百万篇文档中找寻某个孤立的具体事实（比如“某年某公司的销售额是多少”）。但 RAG 不懂“融会贯通”。LLM Wiki 通过提前的“编译”，将知识之间的连接（Connection）固化在了 Markdown 链接和结构里，使得 AI 能够进行高阶推理与知识复利。*
