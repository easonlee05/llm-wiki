# LLM Wiki 运行规范 (Schema)

这是一个面向 AI 助手的规范指令文件。当你（AI 助手）在这个仓库中工作时，请严格遵守以下规则，以维护知识库的一致性与健康度。

---

## 1. 核心定位与角色

*   **角色划分**：用户是知识的“输入者与探索者”；你是“编译器与系统维护者”（Bookkeeper）。
*   **核心任务**：你负责提取、整理、重构和交叉链接所有知识。将零碎的原始资料（Raw Sources）逐步“编译”并沉淀为高度网状化、结构化的 Markdown Wiki 页面（The Wiki）。
*   **设计原则**：**KISS（保持简单）**。Wiki 页面应高度原子化（一个页面只讲一个核心概念或实体），通过双向链接 `[[链接]]` 相互网联，避免冗长和信息重复。
*   **命名规范**：**Wiki 页面文件名必须尽量使用中文**（例如 `wiki/项目范围管理.md`），以提供最直观的阅读和检索体验。避免使用无意义的英文缩写或拼音。

---

## 2. 目录架构

```text
llm-wiki/
├── raw/          # [只读层] 存放原始文件（PDF, TXT, Web Clipper 剪藏等）。你只能读取，绝对不能修改。
├── wiki/         # [写入层] 存放由你生成和更新的 Markdown Wiki 页面。限制子目录深度为 1 层，分为：
│   ├── 00_系统索引/        # 存放索引、模板、MOC（内容地图）。
│   ├── 01_项目管理/        # 存放软件项目管理相关内容。
│   ├── 02_方法论/         # 存放学习与认知方法（第一性原理、费曼法等）。
│   ├── 03_技术笔记/        # 存放技术与理论笔记。
│   └── images/            # 统一存放图片（避免图片散落）。
├── schema.md     # [控制层] 本运行规范。
├── index.md      # [检索层] 按分类索引的页面目录。每次新增/修改 Wiki 页面后必须更新此处。
└── log.md        # [审计层] chronological（时间线）日志，记录所有 Ingest、Query、Lint 操作。
```

---

## 3. 操作规范 (Operations)

### 3.1 摄入 (Ingest)
当用户将新资料放入 `raw/` 文件夹并要求你摄入时，请执行以下“编译”流程：
1.  **阅读与分析**：提取新资料中的核心事实、观点、定义及关系。
2.  **更新/新建 Wiki 页面**：
    *   如果资料涉及已有概念，**修改**已有的 `wiki/xxx.md` 页面，合并新内容（如有冲突需标注或与用户确认）。
    *   如果是新概念，在 `wiki/` 下**新建** `wiki/新页面.md`，并在其中使用 `[[其他页面]]` 与现有知识库进行关联。
3.  **遵循 Wiki 页面格式**（参见下文第 4 节）。
4.  **更新索引 `index.md`**：如果创建了新页面，将其添加至相应的分类中。
5.  **记录日志 `log.md`**：添加一条形如 `## [YYYY-MM-DD] ingest | 摄入原始文件 <文件名>` 的日志，并在下方简述更新了哪些 Wiki 页面。

### 3.2 查询 (Query)
当用户向你提问时，请不要盲目扫描所有原始资料。你的检索策略应为：
1.  **首选 Index**：先读取 `index.md` 找到可能相关的 Wiki 页面。
2.  **阅读 Wiki 页面**：读取目标 Wiki 页面及其链入/链出页面，生成高合成度、结构化的回答。
3.  **知识沉淀**：如果用户的提问具有代表性，或生成的对比分析（如对比表格、思维模型）具有长期价值，**主动询问或直接将其新建为 wiki 页面**，让探索成果复利沉淀。

### 3.3 检查与健康维护 (Lint)
定期（或根据用户指令）扫描整个知识库，执行“编译检查”：
1.  **孤儿页面 (Orphan Pages)**：找出没有任何入站链接的 Wiki 页面，并在 `index.md` 或其他相关页面中为其添加链接。
2.  **未建页概念 (Missing Concept Pages)**：如果在某些页面中频繁用普通文本提到某核心概念（例如“第一性原理”），而 `wiki/` 下没有该概念的专属页面，请提示创建它，或直接新建并将相关词汇替换为 `[[第一性原理]]`。
3.  **冲突校验**：发现不同来源或不同 Wiki 页面之间存在信息矛盾时，汇总成“冲突报告”呈报给用户，或在页面中以高亮 `> [!WARNING]` 标注冲突以待厘清。

---

## 4. Wiki 页面格式约定

为了让 Obsidian 能够美观地渲染并保证数据可读性，每个 `wiki/` 目录下的 Markdown 文件必须遵循以下格式：

```markdown
---
tags: [概念, 学习法]          # 使用 YAML frontmatter，便于 Obsidian 过滤和 Dataview 查询
created: YYYY-MM-DD
sources: [raw/示例原始文件.md] # 引用此内容的原始数据源，用于事实追溯
---

# 概念名称

这里是关于该概念的原子化定义。尽量使用简短、精准的语言。

## 核心要点
- 要点一：包含 [[其他相关概念]]。
- 要点二。

## 应用与实践
这里写具体的使用方法或实例。

---
## 关联引用
- 返回：[[index]] | [[其他父页面]]
```

---

## 5. 日志与索引更新模板

*   **log.md 日志行格式**：
    ```markdown
    ## [YYYY-MM-DD] <操作类型: ingest/query/lint> | <简短描述>
    - **修改/新增文件**：
      - `[NEW]` [[wiki/子目录/新页面]]
      - `[MODIFY]` [[wiki/子目录/旧页面]]
    - **更新摘要**：简述本次更新的主要内容。
    ```
*   **index.md 索引页结构**：
    保持 `index.md` 包含一个清晰的分组树，将 Wiki 页面链接分为 `【实体】`、`【方法与模型】`、`【技术与理论】` 等。
